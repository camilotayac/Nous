# Producto Cartesiano

## Pares Ordenados

Un **par ordenado** $(a, b)$ cumple:

$$(a, b) = (c, d) \iff a = c \land b = d$$

## Producto Cartesiano

$$A \times B = \{(a, b) \mid a \in A, b \in B\}$$

Si $|A| = m$ y $|B| = n$, entonces $|A \times B| = m \cdot n$.

## Ejemplos

$A = \{1, 2\}$, $B = \{x, y\}$

$$A \times B = \{(1, x), (1, y), (2, x), (2, y)\}$$

## Relaciones

Una **relación** $R$ de $A$ en $B$ es un subconjunto de $A \times B$:

$$R \subseteq A \times B$$

```{=typst}
#fletcher.diagram(
  node("A"),
  node("B"),
  edge("->", label: "f")
)
```
